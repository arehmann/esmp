package com.esmp.ui;

import com.vaadin.flow.component.html.Div;
import com.vaadin.flow.router.PageTitle;
import com.vaadin.flow.router.Route;

/**
 * Governance dashboard view — stub placeholder until Plan 03 implements the full dashboard.
 *
 * <p>This stub exists so MainLayout can reference DashboardView.class at compile time. Plan 03 will
 * replace this with the full implementation.
 */
@Route(value = "", layout = MainLayout.class)
@PageTitle("ESMP Dashboard")
public class DashboardView extends Div {

  public DashboardView() {
    // Full implementation provided in Plan 03.
  }
}
