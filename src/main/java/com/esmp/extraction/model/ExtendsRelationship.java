package com.esmp.extraction.model;

import org.springframework.data.neo4j.core.schema.GeneratedValue;
import org.springframework.data.neo4j.core.schema.Id;
import org.springframework.data.neo4j.core.schema.RelationshipProperties;
import org.springframework.data.neo4j.core.schema.TargetNode;

/**
 * Relationship properties for an EXTENDS edge from a child {@link ClassNode} to its superclass.
 *
 * <p>The {@code resolutionConfidence} indicates whether the superclass type was successfully
 * resolved to a known class in the extracted source set.
 */
@RelationshipProperties
public class ExtendsRelationship {

  /** Internal relationship ID — generated by Neo4j, not a business key. */
  @Id @GeneratedValue private Long id;

  @TargetNode private ClassNode target;

  /**
   * Whether the superclass type was resolved during extraction.
   * One of {@code "RESOLVED"} (found in source set) or {@code "UNRESOLVED"} (external class).
   */
  private String resolutionConfidence;

  public ExtendsRelationship() {}

  public ExtendsRelationship(ClassNode target, String resolutionConfidence) {
    this.target = target;
    this.resolutionConfidence = resolutionConfidence;
  }

  public Long getId() {
    return id;
  }

  public ClassNode getTarget() {
    return target;
  }

  public void setTarget(ClassNode target) {
    this.target = target;
  }

  public String getResolutionConfidence() {
    return resolutionConfidence;
  }

  public void setResolutionConfidence(String resolutionConfidence) {
    this.resolutionConfidence = resolutionConfidence;
  }
}
