---
phase: 19-alfa-wrapper-recipe-book-deep-transitive-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/migration/alfa-recipe-book-overlay.json
  - src/main/java/com/esmp/extraction/config/MigrationConfig.java
  - src/main/java/com/esmp/migration/application/RecipeBookRegistry.java
  - src/main/java/com/esmp/extraction/visitor/MigrationPatternVisitor.java
  - src/test/java/com/esmp/migration/application/AlfaCatalogOverlayTest.java
  - src/test/java/com/esmp/extraction/visitor/MigrationPatternVisitorAlfaTest.java
  - src/test/java/com/esmp/migration/application/RecipeBookSeedTest.java
autonomous: true
requirements: [ALFA-01, ALFA-05]

must_haves:
  truths:
    - "RecipeBookRegistry loads 150+ Alfa* rules from classpath at startup without manual configuration"
    - "All 10 wrapper categories are represented in the overlay"
    - "NEEDS_MAPPING entries for AlfaStyloPanel, DTPEditorPanel, AlfaColorChooser, AlfaCalendarWindow are present"
    - "MigrationPatternVisitor produces a MigrationActionData for a class importing com.alfa.ui.AlfaButton"
    - "Reload endpoint re-ingests the Alfa* overlay without restart"
  artifacts:
    - path: "src/main/resources/migration/alfa-recipe-book-overlay.json"
      provides: "150+ Alfa* migration rules in RecipeRule JSON format"
    - path: "src/main/java/com/esmp/extraction/config/MigrationConfig.java"
      provides: "alfaOverlayPath property defaulting to classpath:/migration/alfa-recipe-book-overlay.json"
    - path: "src/main/java/com/esmp/migration/application/RecipeBookRegistry.java"
      provides: "Auto-load of Alfa* overlay inside load() after seed, before custom overlay"
    - path: "src/main/java/com/esmp/extraction/visitor/MigrationPatternVisitor.java"
      provides: "com.alfa.* import detection path producing MigrationActionData"
  key_links:
    - from: "RecipeBookRegistry.load()"
      to: "classpath:/migration/alfa-recipe-book-overlay.json"
      via: "getClass().getResourceAsStream after base seed load"
    - from: "MigrationPatternVisitor.processImport()"
      to: "rulesBySource lookup"
      via: "direct lookup for com.alfa.* then fallback COMPLEX_REWRITE/NO"
---

<objective>
Alfa* catalog JSON overlay (150+ rules, all 10 categories), auto-load in RecipeBookRegistry,
MigrationPatternVisitor detects com.alfa.* imports, NEEDS_MAPPING entries for GWT/spike classes.

Purpose: 1,076 Vaadin-affected classes in adsuite-market use Alfa* wrappers — they are invisible
to the recipe engine because only com.vaadin.* imports are detected. This plan creates the full
Alfa* catalog and wires it into the existing load/detect pipeline so plans 19-02 and 19-03 can
build deep transitive detection and updated API responses on top.

Output: alfa-recipe-book-overlay.json, MigrationConfig.alfaOverlayPath, RecipeBookRegistry
auto-load, MigrationPatternVisitor com.alfa.* detection, 3 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/main/java/com/esmp/migration/api/RecipeRule.java
@src/main/java/com/esmp/migration/application/RecipeBookRegistry.java
@src/main/java/com/esmp/extraction/config/MigrationConfig.java
@src/main/java/com/esmp/extraction/visitor/MigrationPatternVisitor.java
@src/main/resources/migration/vaadin-recipe-book-seed.json
@src/test/java/com/esmp/migration/application/RecipeBookSeedTest.java
@src/test/java/com/esmp/migration/application/RecipeBookRegistryTest.java
@src/test/resources/migration/test-recipe-book.json

<interfaces>
From RecipeRule.java (record — isBase NOT stored in JSON, set by registry at load time):
  String id, category, source, target, actionType, automatable, context,
  List<String> migrationSteps, String status, int usageCount, String discoveredAt, boolean isBase

RecipeBookSeedTest.allCategoriesAreValid() currently accepts only:
  {COMPONENT, DATA_BINDING, SERVER, JAVAX_JAKARTA, DISCOVERED}
Task 3 must update VALID_CATEGORIES to also include all 10 ALFA_* category strings.
The seed test reads only vaadin-recipe-book-seed.json — overlay categories do not break the
existing test, but VALID_CATEGORIES must be updated so it accurately documents allowed values.

RecipeBookRegistry.load() current sequence:
  1. Copy seed to runtime file if missing
  2. Read runtime file -> merged map, mark isBase=true
  3. Read customRecipeBookPath overlay if configured
  NEW step 2b (between 2 and 3): Read Alfa* overlay from alfaOverlayPath classpath resource,
  mark isBase=false. Strip "classpath:" prefix, call getClass().getResourceAsStream().
  Log WARN if resource not found but do NOT throw — allow startup to continue.

MigrationPatternVisitor.processImport() current steps:
  1. Direct lookup by source FQN (handles known com.alfa.* after overlay is loaded)
  2. JAVAX_JAKARTA prefix matching
  3. Unknown com.vaadin.* fallback -> COMPLEX_REWRITE/NO
  NEW step 4 (after step 3): Unknown com.alfa.* fallback -> COMPLEX_REWRITE/NO with
  context="Unknown Alfa* wrapper type — check if an Alfa* overlay rule covers this"
  Condition: importFqn.startsWith("com.alfa.") and no rule found in steps 1-3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create alfa-recipe-book-overlay.json with 150+ rules across all 10 categories</name>
  <files>src/main/resources/migration/alfa-recipe-book-overlay.json</files>
  <behavior>
    - JSON parses as { "rules": [...] } matching vaadin-recipe-book-seed.json schema
    - Total rule count >= 150 (these are MAPPED-status rules) plus the 4 NEEDS_MAPPING entries
    - All 10 category strings present: ALFA_LAYOUT, ALFA_TABSHEET, ALFA_BUTTON, ALFA_INPUT,
      ALFA_DATETIME, ALFA_TABLE, ALFA_WINDOW, ALFA_PORTAL, ALFA_DND, ALFA_SPECIALIZED
    - All IDs unique; no collision with seed IDs (seed uses COMP-NNN, DATA-NNN, SRV-NNN, JJ-NNN)
    - All source FQNs unique within the overlay
    - MAPPED + CHANGE_TYPE/CHANGE_PACKAGE rules have non-null target
    - isBase field NOT present in JSON
    - migrationSteps: >= 2 entries for CHANGE_TYPE; >= 3 for COMPLEX_REWRITE
    - 4 NEEDS_MAPPING entries with status=NEEDS_MAPPING, actionType=COMPLEX_REWRITE,
      automatable=NO, target=null: AlfaStyloPanel, DTPEditorPanel, AlfaColorChooser, AlfaCalendarWindow
  </behavior>
  <action>
Create src/main/resources/migration/alfa-recipe-book-overlay.json.

The file must contain at least 150 MAPPED rules + 4 NEEDS_MAPPING rules = 154+ total.
Use these exact source FQNs (the targets listed are the required Vaadin 24 FQNs):

ALFA_LAYOUT (IDs ALFA-L-001..N, target at least 15 rules):
  com.alfa.ui.AlfaVerticalLayout -> VerticalLayout (orderedlayout), CHANGE_TYPE, YES
  com.alfa.ui.AlfaHorizontalLayout -> HorizontalLayout (orderedlayout), CHANGE_TYPE, YES
  com.alfa.ui.AlfaFormLayout -> FormLayout (formlayout), CHANGE_TYPE, YES
  com.alfa.ui.AlfaGridLayout -> null, COMPLEX_REWRITE, NO (no direct equiv; use CSS Grid)
  com.alfa.ui.AlfaSplitPanel -> SplitLayout, CHANGE_TYPE, YES
  com.alfa.ui.AlfaPanel -> html.Div, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaAbsoluteLayout -> null, COMPLEX_REWRITE, NO (position via CSS)
  com.alfa.ui.AlfaVerticalSplitPanel -> SplitLayout, CHANGE_TYPE, PARTIAL (set VERTICAL)
  com.alfa.ui.AlfaHorizontalSplitPanel -> SplitLayout, CHANGE_TYPE, YES
  com.alfa.ui.AlfaCssLayout -> html.Div, CHANGE_TYPE, YES
  com.alfa.ui.AlfaOrderedLayout -> VerticalLayout, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaFormItem -> FormLayout, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaFlexLayout -> FlexLayout (orderedlayout), CHANGE_TYPE, YES
  com.alfa.ui.AlfaScrollPanel -> html.Div, COMPLEX_REWRITE, PARTIAL (CSS overflow:auto)
  com.alfa.ui.AlfaMarginInfo -> null, COMPLEX_REWRITE, NO (use CSS margin)
  com.alfa.ui.AlfaWrapLayout -> FlexLayout, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaSidePanel -> html.Div, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaHeaderLayout -> html.Header, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaFooterLayout -> html.Footer, CHANGE_TYPE, PARTIAL

ALFA_TABSHEET (IDs ALFA-T-001..N, at least 7 rules):
  com.alfa.ui.AlfaTabSheet -> tabs.Tabs, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaAccordion -> accordion.Accordion, CHANGE_TYPE, YES
  com.alfa.ui.AlfaTab -> tabs.Tab, CHANGE_TYPE, YES
  com.alfa.ui.AlfaTabContent -> html.Div, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaAccordionPanel -> accordion.AccordionPanel, CHANGE_TYPE, YES
  com.alfa.ui.AlfaWizard -> html.Div, COMPLEX_REWRITE, NO (build multi-step flow manually)
  com.alfa.ui.AlfaWizardStep -> html.Div, COMPLEX_REWRITE, NO

ALFA_BUTTON (IDs ALFA-B-001..N, at least 10 rules):
  com.alfa.ui.AlfaButton -> button.Button, CHANGE_TYPE, YES
  com.alfa.ui.AlfaLink -> html.Anchor, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaMenuBar -> menubar.MenuBar, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaContextMenu -> contextmenu.ContextMenu, CHANGE_TYPE, YES
  com.alfa.ui.AlfaNativeButton -> button.Button, CHANGE_TYPE, YES
  com.alfa.ui.AlfaMenuItem -> menubar.MenuItem, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaContextMenuItem -> contextmenu.MenuItem, CHANGE_TYPE, YES
  com.alfa.ui.AlfaToolBar -> orderedlayout.HorizontalLayout, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaToggleButton -> button.Button, CHANGE_TYPE, PARTIAL (use Checkbox theme)
  com.alfa.ui.AlfaSplitButton -> menubar.MenuBar, COMPLEX_REWRITE, PARTIAL

ALFA_INPUT (IDs ALFA-I-001..N, at least 20 rules):
  com.alfa.ui.AlfaTextField -> textfield.TextField, CHANGE_TYPE, YES
  com.alfa.ui.AlfaTextArea -> textfield.TextArea, CHANGE_TYPE, YES
  com.alfa.ui.AlfaPasswordField -> textfield.PasswordField, CHANGE_TYPE, YES
  com.alfa.ui.AlfaEmailField -> textfield.EmailField, CHANGE_TYPE, YES
  com.alfa.ui.AlfaNumberField -> textfield.NumberField, CHANGE_TYPE, YES
  com.alfa.ui.AlfaIntegerField -> textfield.IntegerField, CHANGE_TYPE, YES
  com.alfa.ui.AlfaComboBox -> combobox.ComboBox, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaSelect -> select.Select, CHANGE_TYPE, YES
  com.alfa.ui.AlfaCheckBox -> checkbox.Checkbox, CHANGE_TYPE, YES
  com.alfa.ui.AlfaCheckBoxGroup -> checkbox.CheckboxGroup, CHANGE_TYPE, YES
  com.alfa.ui.AlfaRadioButtonGroup -> radiobutton.RadioButtonGroup, CHANGE_TYPE, YES
  com.alfa.ui.AlfaSlider -> html.Input, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaOptionGroup -> radiobutton.RadioButtonGroup, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaNativeSelect -> select.Select, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaListBox -> listbox.ListBox, CHANGE_TYPE, YES
  com.alfa.ui.AlfaMultiSelect -> listbox.MultiSelectListBox, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaTokenField -> combobox.MultiSelectComboBox, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaAutoComplete -> combobox.ComboBox, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaSearchField -> textfield.TextField, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaInlineEditor -> html.Div, COMPLEX_REWRITE, NO

ALFA_DATETIME (IDs ALFA-D-001..N, at least 10 rules):
  com.alfa.ui.AlfaDateField -> datepicker.DatePicker, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaDateTimeField -> datetimepicker.DateTimePicker, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaInlineDateField -> datepicker.DatePicker, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaTimeField -> timepicker.TimePicker, CHANGE_TYPE, YES
  com.alfa.ui.AlfaRichTextArea -> richtexteditor.RichTextEditor, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaPopupDateField -> datepicker.DatePicker, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaDateRangeField -> null, COMPLEX_REWRITE, NO (two DatePickers)
  com.alfa.ui.AlfaMonthCalendar -> datepicker.DatePicker, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaYearPicker -> datepicker.DatePicker, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaWeekPicker -> datepicker.DatePicker, COMPLEX_REWRITE, NO

ALFA_TABLE (IDs ALFA-TB-001..N, at least 14 rules):
  com.alfa.ui.AlfaTable -> grid.Grid, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaTreeTable -> treegrid.TreeGrid, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaTree -> treegrid.TreeGrid, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaGrid -> grid.Grid, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaListSelect -> listbox.ListBox, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaTwinColSelect -> listbox.MultiSelectListBox, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaTableFieldFactory -> grid.editor.Editor, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaContainer -> data.provider.DataProvider, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaBeanItemContainer -> data.provider.ListDataProvider, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaIndexedContainer -> data.provider.ListDataProvider, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaPagedTable -> grid.Grid, COMPLEX_REWRITE, PARTIAL (use Grid lazy loading)
  com.alfa.ui.AlfaLazyLoadingTable -> grid.Grid, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaEditableGrid -> grid.Grid, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaSortableTable -> grid.Grid, CHANGE_TYPE, PARTIAL

ALFA_WINDOW (IDs ALFA-W-001..N, at least 10 rules):
  com.alfa.ui.AlfaWindow -> dialog.Dialog, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaSubWindow -> dialog.Dialog, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaPopupView -> popover.Popover, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaNotification -> notification.Notification, CHANGE_TYPE, YES
  com.alfa.ui.AlfaConfirmDialog -> confirmdialog.ConfirmDialog, CHANGE_TYPE, YES
  com.alfa.ui.AlfaErrorDialog -> dialog.Dialog, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaProgressDialog -> dialog.Dialog, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaMessageBox -> dialog.Dialog, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaTooltip -> html.Span, COMPLEX_REWRITE, PARTIAL (use title attr or tooltip add-on)
  com.alfa.ui.AlfaBalloon -> popover.Popover, COMPLEX_REWRITE, PARTIAL

ALFA_PORTAL (IDs ALFA-P-001..N, at least 5 rules):
  com.alfa.ui.portal.AlfaPortlet -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.portal.AlfaPortletLayout -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.portal.AlfaPortletWindow -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.portal.AlfaPortalHeader -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.portal.AlfaDashboard -> orderedlayout.VerticalLayout, COMPLEX_REWRITE, NO

ALFA_DND (IDs ALFA-DND-001..N, at least 7 rules):
  com.alfa.ui.dnd.AlfaDragAndDropWrapper -> dnd.DragSource, COMPLEX_REWRITE, NO
  com.alfa.ui.dnd.AlfaDropHandler -> dnd.DropTarget, COMPLEX_REWRITE, NO
  com.alfa.ui.dnd.AlfaDragSource -> dnd.DragSource, CHANGE_TYPE, PARTIAL
  com.alfa.ui.dnd.AlfaDropTarget -> dnd.DropTarget, CHANGE_TYPE, PARTIAL
  com.alfa.ui.dnd.AlfaDragEvent -> dnd.DragStartEvent, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.dnd.AlfaDropEvent -> dnd.DropEvent, CHANGE_TYPE, PARTIAL
  com.alfa.ui.dnd.AlfaSortable -> dnd.DragSource, COMPLEX_REWRITE, NO

ALFA_SPECIALIZED (IDs ALFA-S-001..N, at least 20 rules):
  com.alfa.ui.AlfaLabel -> html.Span, CHANGE_TYPE, YES
  com.alfa.ui.AlfaImage -> html.Image, CHANGE_TYPE, YES
  com.alfa.ui.AlfaProgressBar -> progressbar.ProgressBar, CHANGE_TYPE, YES
  com.alfa.ui.AlfaUpload -> upload.Upload, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaVideo -> html.NativeDetails, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaAudio -> html.Div, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaFlash -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaEmbedded -> html.IFrame, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaCustomComponent -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaCustomLayout -> html.Div, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaIcon -> icon.VaadinIcon, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaHtmlLabel -> Html, CHANGE_TYPE, PARTIAL
  com.alfa.ui.AlfaChart -> null, COMPLEX_REWRITE, NO (use third-party chart add-on)
  com.alfa.ui.AlfaMap -> null, COMPLEX_REWRITE, NO (use third-party map add-on)
  com.alfa.ui.AlfaCodeEditor -> html.Div, COMPLEX_REWRITE, NO (embed third-party code editor)
  com.alfa.ui.AlfaMarkdown -> Html, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaHtmlEditor -> richtexteditor.RichTextEditor, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaColorPicker -> null, COMPLEX_REWRITE, NO
  com.alfa.ui.AlfaFileDownloader -> html.Anchor, COMPLEX_REWRITE, PARTIAL
  com.alfa.ui.AlfaExternalResource -> html.Anchor, CHANGE_TYPE, PARTIAL

NEEDS_MAPPING (IDs ALFA-NM-001..004, status=NEEDS_MAPPING):
  com.alfa.ui.AlfaStyloPanel — GWT-based styled panel, no Flow equivalent
  com.alfa.ui.DTPEditorPanel — custom DTP editor, no Flow equivalent
  com.alfa.ui.AlfaColorChooser — color picker, no direct Flow equivalent
  com.alfa.ui.AlfaCalendarWindow — calendar popup, use DatePicker or custom component

All 10 categories must be populated. Use full Flow FQNs (e.g.
com.vaadin.flow.component.button.Button, not just "button.Button" — the shorthand above is
used in this action only for readability). usageCount=0, discoveredAt=null for all rules.
Rule count check: 19+7+10+20+10+14+10+5+7+20 = 122 base + extras to reach 150. Add remaining
rules to categories that still have room (ALFA_INPUT, ALFA_SPECIALIZED, ALFA_LAYOUT) with
plausible Alfa* wrapper type names, keeping all source FQNs unique.
  </action>
  <verify>
    <automated>cd /c/frontoffice/esmp &amp;&amp; python3 -c "
import json
with open('src/main/resources/migration/alfa-recipe-book-overlay.json') as f:
    d = json.load(f)
r = d['rules']
ids = [x['id'] for x in r]
srcs = [x['source'] for x in r]
cats = set(x['category'] for x in r)
nm = [x for x in r if x['status']=='NEEDS_MAPPING']
ec = {'ALFA_LAYOUT','ALFA_TABSHEET','ALFA_BUTTON','ALFA_INPUT','ALFA_DATETIME',
      'ALFA_TABLE','ALFA_WINDOW','ALFA_PORTAL','ALFA_DND','ALFA_SPECIALIZED'}
assert len(r)&gt;=150,f'Need 150+, got {len(r)}'
assert len(set(ids))==len(ids),'dup IDs'
assert len(set(srcs))==len(srcs),'dup sources'
assert ec.issubset(cats),f'missing cats:{ec-cats}'
assert len(nm)&gt;=4,'need 4 NEEDS_MAPPING'
nms={x['source'] for x in nm}
for s in ['com.alfa.ui.AlfaStyloPanel','com.alfa.ui.DTPEditorPanel','com.alfa.ui.AlfaColorChooser','com.alfa.ui.AlfaCalendarWindow']:
    assert s in nms,f'missing {s}'
print('PASS',len(r),'rules')
"
  </verify>
  <done>
    alfa-recipe-book-overlay.json exists at src/main/resources/migration/; python3 verify
    script prints "PASS"; total rule count >= 150; all 10 ALFA_* categories present;
    4 NEEDS_MAPPING entries (AlfaStyloPanel, DTPEditorPanel, AlfaColorChooser, AlfaCalendarWindow).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add alfaOverlayPath to MigrationConfig; wire Alfa* overlay into RecipeBookRegistry.load(); add com.alfa.* detection to MigrationPatternVisitor</name>
  <files>
    src/main/java/com/esmp/extraction/config/MigrationConfig.java,
    src/main/java/com/esmp/migration/application/RecipeBookRegistry.java,
    src/main/java/com/esmp/extraction/visitor/MigrationPatternVisitor.java
  </files>
  <action>
**MigrationConfig.java** - add one new field after customRecipeBookPath, keeping all existing
fields/getters/setters unchanged. Also update the Javadoc example to include the new property.

New field to insert after customRecipeBookPath field:

  /** Path to the built-in Alfa* overlay JSON. Prefix "classpath:" triggers classpath loading.
   *  Default: "classpath:/migration/alfa-recipe-book-overlay.json". */
  private String alfaOverlayPath = "classpath:/migration/alfa-recipe-book-overlay.json";
  public String getAlfaOverlayPath() { return alfaOverlayPath; }
  public void setAlfaOverlayPath(String alfaOverlayPath) { this.alfaOverlayPath = alfaOverlayPath; }

---

**RecipeBookRegistry.java** - in load(), after the base rules log.info line and before the
existing custom-overlay block, insert step 2b:

  String alfaPath = migrationConfig.getAlfaOverlayPath();
  if (alfaPath != null && !alfaPath.isBlank()) {
      loadOverlayFromClasspath(alfaPath, merged);
  }

Add the following private helper after the existing withIsBase helper at the bottom of the class.
The method strips the "classpath:" prefix and calls getClass().getResourceAsStream(). It logs a
WARN if the resource is not found but does NOT throw, allowing startup to proceed:

  private void loadOverlayFromClasspath(String resourcePath, Map<String, RecipeRule> merged) {
      String resolved = resourcePath.startsWith("classpath:")
          ? resourcePath.substring("classpath:".length()) : resourcePath;
      try (InputStream in = getClass().getResourceAsStream(resolved)) {
          if (in == null) {
              log.warn("Alfa overlay resource not found: {} - skipping", resolved);
              return;
          }
          RecipeBook overlayBook = objectMapper.readValue(in, RecipeBook.class);
          int count = 0;
          for (RecipeRule rule : overlayBook.rules()) {
              merged.put(rule.source(), withIsBase(rule, false));
              count++;
          }
          log.info("Loaded {} Alfa* overlay rules from classpath:{}", count, resolved);
      } catch (IOException e) {
          log.warn("Failed to load Alfa overlay from {}: {} - skipping", resolved, e.getMessage());
      }
  }

---

**MigrationPatternVisitor.java** - append step 4 at the bottom of processImport(), after the
existing step 3 block. Step 4 fires only for com.alfa.* imports not matched by step 1:

  // 4. Unknown com.alfa.* type - not found in registry
  if (importFqn.startsWith("com.alfa.")) {
      acc.addMigrationAction(classFqn, new MigrationActionData(
          ActionType.COMPLEX_REWRITE,
          importFqn,
          "Unknown Alfa* type - manual investigation required",
          Automatable.NO,
          "Unknown Alfa* wrapper type - check if an Alfa* overlay rule covers this"
      ));
  }

Update the class-level Javadoc Detection Strategy numbered list to add item 4:
  4. Unknown com.alfa.* type (not in registry) - produces COMPLEX_REWRITE/NO
  </action>
  <verify>
    <automated>cd /c/frontoffice/esmp &amp;&amp; ./gradlew compileJava -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    All 3 files compile cleanly. MigrationConfig has getAlfaOverlayPath(). RecipeBookRegistry
    calls loadOverlayFromClasspath() in load(). MigrationPatternVisitor has the com.alfa.* step 4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write AlfaCatalogOverlayTest and MigrationPatternVisitorAlfaTest; update RecipeBookSeedTest</name>
  <files>
    src/test/java/com/esmp/migration/application/AlfaCatalogOverlayTest.java,
    src/test/java/com/esmp/extraction/visitor/MigrationPatternVisitorAlfaTest.java,
    src/test/java/com/esmp/migration/application/RecipeBookSeedTest.java
  </files>
  <behavior>
    AlfaCatalogOverlayTest (9 tests):
    - overlayContainsAtLeast150Rules
    - overlayCoversAllTenCategories: asserts Set of all 10 ALFA_* category strings is a subset
      of the categories present in the overlay
    - overlayHasNoNullIds
    - overlayHasNoDuplicateIds
    - overlayHasNoDuplicateSources
    - overlayNeedsMappingEntriesPresent: 4 sources (AlfaStyloPanel, DTPEditorPanel,
      AlfaColorChooser, AlfaCalendarWindow) all present with status=NEEDS_MAPPING
    - overlayMappedChangeTypeRulesHaveNonNullTarget
    - registryLoadsAlfaOverlayAutomatically: RecipeBookRegistry(@TempDir runtime, no custom
      overlay, alfaOverlayPath=classpath:...) -> load() -> size > 80 AND
      findBySource("com.alfa.ui.AlfaButton").isPresent()
    - alfaRulesHaveIsBaseFalse: rules whose source starts with "com.alfa." all have isBase=false

    MigrationPatternVisitorAlfaTest (3 tests):
    - knownAlfaButtonProducesMigrationAction: source "package com.test; import com.alfa.ui.AlfaButton;
      public class TestView { AlfaButton btn; }" -> MigrationActionData with
      source=com.alfa.ui.AlfaButton, actionType=CHANGE_TYPE (from ALFA-B-001 rule)
    - unknownAlfaTypeProducesFallbackAction: source imports com.alfa.ui.AlfaUnknownWidget99 ->
      MigrationActionData with actionType=COMPLEX_REWRITE, automatable=NO
    - vaadinImportStillDetected: source imports com.vaadin.ui.Button -> MigrationActionData
      with source=com.vaadin.ui.Button (existing behavior unaffected)

    RecipeBookSeedTest (update only):
    - VALID_CATEGORIES Set.of() must include all 10 ALFA_* strings plus existing 5 values.
    - No other changes. All existing tests continue to pass.
  </behavior>
  <action>
**AlfaCatalogOverlayTest.java** - package com.esmp.migration.application.
Structure identically to RecipeBookSeedTest: @BeforeAll loads the overlay JSON via
ObjectMapper + getResourceAsStream("/migration/alfa-recipe-book-overlay.json").
For registryLoadsAlfaOverlayAutomatically: build a MigrationConfig with @TempDir-backed
recipeBookPath, empty customRecipeBookPath, and alfaOverlayPath set to the classpath path.
Construct RecipeBookRegistry(config, new ObjectMapper()), call load(), then assert.

**MigrationPatternVisitorAlfaTest.java** - package com.esmp.extraction.visitor.
Build the registry by pointing MigrationConfig at a @TempDir runtime file with
alfaOverlayPath="classpath:/migration/alfa-recipe-book-overlay.json". This ensures
the AlfaButton rule (ALFA-B-001) is present for the knownAlfaButton test.

Parse synthetic source strings using the OpenRewrite JavaParser pattern:
  List<SourceFile> parsed = JavaParser.fromJavaVersion().build()
      .parse(List.of(sourceCode), null, new InMemoryExecutionContext());
  ExtractionAccumulator acc = new ExtractionAccumulator();
  new MigrationPatternVisitor(registry).visit(parsed.get(0), acc);
  Map<String, List<MigrationActionData>> actions = acc.getMigrationActions();

Check existing visitor test classes or integration tests for the exact import statements
(openrewrite.java.JavaParser, openrewrite.InMemoryExecutionContext, etc.) before writing.

**RecipeBookSeedTest.java** - locate VALID_CATEGORIES and replace with:

  private static final Set<String> VALID_CATEGORIES = Set.of(
      "COMPONENT", "DATA_BINDING", "SERVER", "JAVAX_JAKARTA", "DISCOVERED",
      "ALFA_LAYOUT", "ALFA_TABSHEET", "ALFA_BUTTON", "ALFA_INPUT", "ALFA_DATETIME",
      "ALFA_TABLE", "ALFA_WINDOW", "ALFA_PORTAL", "ALFA_DND", "ALFA_SPECIALIZED"
  );
  </action>
  <verify>
    <automated>cd /c/frontoffice/esmp &amp;&amp; ./gradlew test --tests "com.esmp.migration.application.AlfaCatalogOverlayTest" --tests "com.esmp.extraction.visitor.MigrationPatternVisitorAlfaTest" --tests "com.esmp.migration.application.RecipeBookSeedTest" -q 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    AlfaCatalogOverlayTest: 9/9 green.
    MigrationPatternVisitorAlfaTest: 3/3 green.
    RecipeBookSeedTest: all existing tests pass; VALID_CATEGORIES updated to include ALFA_* values.
  </done>
</task>

</tasks>

<verification>
1. alfa-recipe-book-overlay.json: python3 integrity check passes (150+ rules, 10 categories, 4 NEEDS_MAPPING)
2. ./gradlew compileJava: clean, zero errors
3. AlfaCatalogOverlayTest: 9/9 green
4. MigrationPatternVisitorAlfaTest: 3/3 green
5. RecipeBookSeedTest: all existing tests pass
6. Application startup log shows "Loaded NNN Alfa* overlay rules from classpath:/migration/alfa-recipe-book-overlay.json"
</verification>

<success_criteria>
- alfa-recipe-book-overlay.json on classpath with >= 150 MAPPED rules and 4 NEEDS_MAPPING entries
- RecipeBookRegistry auto-loads Alfa* overlay on every startup and reload() call without configuration
- MigrationPatternVisitor emits MigrationActionData for both known (registry lookup) and unknown
  (fallback COMPLEX_REWRITE/NO) com.alfa.* imports
- All tests pass
- ALFA-01 satisfied: recipe book contains 150+ Alfa* mappings across all 10 wrapper categories
- ALFA-05 satisfied: existing reload() endpoint re-ingests Alfa* overlay (reload() -> load() -> step 2b)
</success_criteria>

<output>
After completion, create .planning/phases/19-alfa-wrapper-recipe-book-deep-transitive-detection/19-01-SUMMARY.md
</output>
