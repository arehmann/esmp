---
phase: 09-golden-module-pilot
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/main/java/com/esmp/pilot/api/PilotController.java
  - src/main/java/com/esmp/vector/api/VectorSearchController.java
  - src/test/java/com/esmp/pilot/application/PilotServiceIntegrationTest.java
  - src/test/java/com/esmp/vector/application/VectorSearchIntegrationTest.java
autonomous: true
requirements: [GMP-01, GMP-02, GMP-03]

must_haves:
  truths:
    - "GET /api/pilot/recommend returns ranked module recommendations with scores and rationale"
    - "GET /api/pilot/validate/{module} returns a complete validation report with graph checks, module metrics, pilot checks, and markdown report"
    - "POST /api/vector/search accepts a text query and returns ranked chunks with enriched payloads from Qdrant"
    - "Integration tests verify the full pipeline: synthetic fixtures loaded in Neo4j, chunked, indexed in Qdrant, searchable, and validatable"
    - "Golden regression assertions permanently verify pilot module chunk count, enrichment fields, and search relevance"
  artifacts:
    - path: "src/main/java/com/esmp/pilot/api/PilotController.java"
      provides: "REST endpoints GET /api/pilot/recommend and GET /api/pilot/validate/{module}"
      min_lines: 30
    - path: "src/main/java/com/esmp/vector/api/VectorSearchController.java"
      provides: "REST endpoint POST /api/vector/search"
      min_lines: 20
    - path: "src/test/java/com/esmp/pilot/application/PilotServiceIntegrationTest.java"
      provides: "Full pipeline integration tests for GMP-01 and GMP-03"
      min_lines: 150
    - path: "src/test/java/com/esmp/vector/application/VectorSearchIntegrationTest.java"
      provides: "Vector search integration tests for GMP-02"
      min_lines: 80
  key_links:
    - from: "src/main/java/com/esmp/pilot/api/PilotController.java"
      to: "src/main/java/com/esmp/pilot/application/PilotService.java"
      via: "constructor injection"
      pattern: "pilotService\\.recommendModules|pilotService\\.validateModule"
    - from: "src/main/java/com/esmp/vector/api/VectorSearchController.java"
      to: "src/main/java/com/esmp/vector/application/VectorSearchService.java"
      via: "constructor injection"
      pattern: "vectorSearchService\\.search"
    - from: "PilotServiceIntegrationTest"
      to: "Neo4j + Qdrant Testcontainers"
      via: "full pipeline: create nodes -> link -> risk -> index -> validate"
      pattern: "@Testcontainers|@DynamicPropertySource"
---

<objective>
Wire up REST controllers for pilot and vector search endpoints, then create comprehensive integration tests that exercise the full ESMP pipeline end-to-end on synthetic pilot module data.

Purpose: This plan connects the service layer (Plan 01) to REST endpoints and proves the entire pipeline works correctly via integration tests. The tests serve as permanent golden regression tests ensuring pipeline correctness as the codebase evolves.

Output: PilotController, VectorSearchController, PilotServiceIntegrationTest, VectorSearchIntegrationTest
</objective>

<execution_context>
@C:/Users/aziz.rehman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aziz.rehman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-golden-module-pilot/09-CONTEXT.md
@.planning/phases/09-golden-module-pilot/09-RESEARCH.md
@.planning/phases/09-golden-module-pilot/09-01-SUMMARY.md
@.planning/phases/08-smart-chunking-vector-indexing/08-02-SUMMARY.md

<interfaces>
<!-- Key types created in Plan 01 that this plan consumes -->

From src/main/java/com/esmp/pilot/application/PilotService.java:
```java
@Service
public class PilotService {
  public List<ModuleRecommendation> recommendModules();
  public PilotValidationReport validateModule(String moduleName);
}
```

From src/main/java/com/esmp/vector/application/VectorSearchService.java:
```java
@Service
public class VectorSearchService {
  public SearchResponse search(SearchRequest request);
}
```

From src/main/java/com/esmp/pilot/api/ModuleRecommendation.java:
```java
public record ModuleRecommendation(String moduleName, int classCount, int vaadin7Count,
    double avgEnhancedRiskScore, double riskDiversity, double score, String rationale) {}
```

From src/main/java/com/esmp/pilot/api/PilotValidationReport.java:
```java
public record PilotValidationReport(String generatedAt, String pilotModule,
    ValidationReport graphValidation, int classCount, int vaadin7ClassCount,
    int chunkCount, double avgEnhancedRiskScore, int businessTermCount,
    double domainTermCoveragePercent, List<PilotCheck> pilotChecks, String markdownReport) {}
```

From src/main/java/com/esmp/vector/api/SearchRequest.java:
```java
public record SearchRequest(String query, Integer limit, String module, String stereotype, String chunkType) {}
```

From src/main/java/com/esmp/vector/api/SearchResponse.java:
```java
public record SearchResponse(List<ChunkSearchResult> results, int totalReturned, String query) {}
```

From src/main/java/com/esmp/vector/application/VectorIndexingService.java:
```java
public IndexStatusResponse indexAll(String sourceRoot);
```

From src/main/java/com/esmp/graph/validation/ValidationService.java:
```java
// runAllValidations() returns ValidationReport
```

Existing integration test pattern from VectorIndexingServiceIntegrationTest:
- Uses @SpringBootTest(webEnvironment = WebEnvironment.MOCK)
- @Testcontainers with Neo4j, MySQL, Qdrant containers
- @DynamicPropertySource for container URIs
- Neo4jClient for synthetic data setup via CREATE Cypher
- @TempDir for synthetic .java source files
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PilotController, VectorSearchController, and integration tests</name>
  <files>
    src/main/java/com/esmp/pilot/api/PilotController.java
    src/main/java/com/esmp/vector/api/VectorSearchController.java
    src/test/java/com/esmp/pilot/application/PilotServiceIntegrationTest.java
    src/test/java/com/esmp/vector/application/VectorSearchIntegrationTest.java
  </files>
  <action>
    **1. Create `PilotController` in `com.esmp.pilot.api`:**

    `@RestController @RequestMapping("/api/pilot")` with constructor-injected `PilotService`.

    Endpoints:
    - `@GetMapping("/recommend")` returning `ResponseEntity<List<ModuleRecommendation>>`:
      - Calls `pilotService.recommendModules()`, returns 200 with the list
    - `@GetMapping("/validate/{module}")` returning `ResponseEntity<PilotValidationReport>`:
      - Path variable `module` (use `{module:.+}` regex to prevent dot truncation per Phase 3 convention)
      - Calls `pilotService.validateModule(module)`, returns 200 with the report

    **2. Create `VectorSearchController` in `com.esmp.vector.api`:**

    `@RestController @RequestMapping("/api/vector")` with constructor-injected `VectorSearchService`.

    Endpoint:
    - `@PostMapping("/search")` accepting `@RequestBody SearchRequest` returning `ResponseEntity<SearchResponse>`:
      - Validates query is not blank (return 400 if blank)
      - Calls `vectorSearchService.search(request)`, returns 200 with the response

    **3. Create `PilotServiceIntegrationTest`:**

    Full pipeline integration test using `@SpringBootTest(webEnvironment = WebEnvironment.MOCK)` + `@Testcontainers`.

    Container setup (copy pattern from VectorIndexingServiceIntegrationTest):
    - `Neo4jContainer<?>` with "neo4j:5" image
    - `MySQLContainer<?>` with "mysql:8.0" image
    - `GenericContainer<?>` for Qdrant with "qdrant/qdrant:latest" image, port 6334
    - `@DynamicPropertySource` mapping all container URIs/ports

    Autowire: `PilotService`, `Neo4jClient`, `VectorIndexingService`, `LinkingService`, `RiskService`, `QdrantClient`, `VectorConfig`.

    Use `@TempDir Path tempDir` for synthetic source files.

    **@BeforeAll or @BeforeEach setup:**
    1. Write synthetic `.java` source files from `src/test/resources/fixtures/pilot/` to `tempDir` — read each fixture file and write to tempDir maintaining flat structure. Store paths.
    2. Create Neo4j nodes via Cypher for 20 pilot module classes with correct properties:
       - All nodes: `fullyQualifiedName`, `simpleName`, `packageName='com.esmp.pilot'`, `module='pilot'`, `sourceFilePath` (relative path within tempDir)
       - Use `:JavaClass:Service` for service classes, `:JavaClass:Repository` for repos, `:JavaClass:VaadinView` for views, `:JavaClass:VaadinDataBinding` for forms
       - Include `contentHash`, `structuralRiskScore`, `enhancedRiskScore`, `domainCriticality`, `securitySensitivity`, `financialInvolvement`, `businessRuleDensity` on each
       - Create `:JavaMethod` nodes with `DECLARES_METHOD` edges for key methods
       - Create DEPENDS_ON, CALLS, EXTENDS, IMPLEMENTS edges between appropriate classes
       - Create `:BusinessTerm` nodes and `USES_TERM` edges for domain terms (Invoice, Payment, Customer, Audit)
       - Create `:DBTable` nodes and `MAPS_TO_TABLE` edges for entities
    3. Call `vectorIndexingService.indexAll(tempDir.toString())` to chunk and index

    **Tests (minimum 8):**

    For GMP-01:
    - `recommendModules_returnsPilotModule`: Assert pilot module appears in results with classCount >= 15, vaadin7Count > 0
    - `validateModule_returnsCompleteReport`: Assert PilotValidationReport has non-null graphValidation, classCount matching Neo4j, chunkCount > 0
    - `validateModule_allPilotChecksPass`: Assert all PilotCheck entries have status "PASS"
    - `validateModule_markdownReportGenerated`: Assert markdownReport contains expected section headers

    For GMP-03:
    - `validateModule_riskScoresPopulated`: Assert avgEnhancedRiskScore > 0
    - `validateModule_businessTermsCovered`: Assert businessTermCount > 0 and domainTermCoveragePercent > 0
    - `validateModule_migrationReadinessInReport`: Assert markdownReport contains "Migration Readiness Assessment"

    For pipeline integrity:
    - `fullPipeline_syntheticModuleIndexed`: Assert indexAll returned filesProcessed > 0, chunksIndexed > 0

    **4. Create `VectorSearchIntegrationTest`:**

    Same Testcontainers setup as PilotServiceIntegrationTest (or share a base class if practical — but separate is fine and clearer).

    Autowire: `VectorSearchService`, `VectorIndexingService`, `Neo4jClient`, `QdrantClient`, `VectorConfig`.

    Setup: Same Neo4j node creation + indexAll as PilotServiceIntegrationTest (can be a subset — at least 5 classes to make search meaningful).

    **Tests (minimum 5) for GMP-02:**

    - `search_byServiceQuery_returnsServiceChunks`: Search "invoice processing service" with no filters, assert top results include InvoiceService-related chunks
    - `search_byRepositoryQuery_returnsRepoChunks`: Search "customer data repository" — assert CustomerRepository appears in results
    - `search_withModuleFilter_scopesToPilot`: Search with `module="pilot"`, assert all results have module="pilot"
    - `search_withStereotypeFilter_filtersCorrectly`: Search with `stereotype="Service"`, assert results are service chunks
    - `search_resultsHaveEnrichmentPayloads`: For any search result, assert `classFqn` not null, `enhancedRiskScore` present, `chunkType` is CLASS_HEADER or METHOD

    Each test should use AssertJ assertions. Use `@Test @DisplayName` for clear test naming.

    CRITICAL NOTES:
    - WebEnvironment.MOCK is required (not NONE) because Vaadin SpringBootAutoConfiguration needs WebApplicationContext
    - The `sourceFilePath` stored in Neo4j nodes must be relative paths that resolve against the `sourceRoot` passed to `indexAll()` — e.g., if tempDir is the sourceRoot, then sourceFilePath = "InvoiceService.java" (flat in tempDir)
    - The Qdrant search `searchAsync` returns `ScoredPoint` — payload access is via `getPayloadMap().get("fieldName").getStringValue()` etc.
    - Remember to wait for indexAll to complete before running search tests
    - If both test classes need the same setup, consider extracting common helper methods but keep them in separate classes for clarity
  </action>
  <verify>
    <automated>cd /c/frontoffice/esmp && ./gradlew test --tests "com.esmp.pilot.*" --tests "com.esmp.vector.application.VectorSearchIntegrationTest" 2>&1 | tail -20</automated>
  </verify>
  <done>
    PilotController exposes GET /api/pilot/recommend and GET /api/pilot/validate/{module}. VectorSearchController exposes POST /api/vector/search. PilotServiceIntegrationTest (8+ tests) verifies full pipeline: synthetic module creation, validation report completeness, pilot checks passing, risk scores populated, business terms covered, markdown report generated. VectorSearchIntegrationTest (5+ tests) verifies search relevance, module/stereotype filtering, enrichment payload presence. All tests green.
  </done>
</task>

</tasks>

<verification>
- `./gradlew compileJava` succeeds
- `./gradlew test --tests "com.esmp.pilot.*"` — all PilotServiceIntegrationTest tests pass
- `./gradlew test --tests "com.esmp.vector.application.VectorSearchIntegrationTest"` — all search tests pass
- GET /api/pilot/recommend returns JSON array with module recommendations
- GET /api/pilot/validate/pilot returns JSON with graphValidation, pilotChecks, markdownReport
- POST /api/vector/search with `{"query":"invoice service","limit":5}` returns ranked results
</verification>

<success_criteria>
- All integration tests pass (13+ tests total across both test classes)
- PilotController and VectorSearchController return correct HTTP responses
- Full pipeline validated: Neo4j nodes -> chunking -> Qdrant indexing -> search -> validation report
- Golden regression tests permanently verify: chunk count > 0 for pilot module, enrichment fields present in search results, risk scores populated in validation report
- Markdown validation report contains all required sections (Module Overview, Graph Validation, Pilot Checks, Migration Readiness Assessment)
</success_criteria>

<output>
After completion, create `.planning/phases/09-golden-module-pilot/09-02-SUMMARY.md`
</output>
