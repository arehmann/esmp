---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/test/java/com/esmp/infrastructure/health/HealthIndicatorIntegrationTest.java
  - src/test/java/com/esmp/infrastructure/health/VirtualThreadsTest.java
  - src/test/java/com/esmp/infrastructure/startup/DataStoreStartupValidatorTest.java
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "All three health indicators (Neo4j, MySQL, Qdrant) report UP in integration test with real containers"
    - "Virtual threads are active in the running Spring Boot application"
    - "Actuator health endpoint returns 200 with all components UP"
    - "Flyway migration applies successfully against real MySQL container"
    - "docker compose up starts all services and they become healthy"
    - "Application startup fails if a data store is unreachable"
  artifacts:
    - path: "src/test/java/com/esmp/infrastructure/health/HealthIndicatorIntegrationTest.java"
      provides: "Integration test proving all health indicators UP with Testcontainers"
      contains: "@SpringBootTest"
      min_lines: 40
    - path: "src/test/java/com/esmp/infrastructure/health/VirtualThreadsTest.java"
      provides: "Test proving virtual threads enabled and health endpoint accessible"
      contains: "virtual"
      min_lines: 20
    - path: "src/test/java/com/esmp/infrastructure/startup/DataStoreStartupValidatorTest.java"
      provides: "Unit test proving fail-fast startup validation aborts when a health indicator returns DOWN"
      contains: "DataStoreStartupValidator"
      min_lines: 25
  key_links:
    - from: "src/test/java/com/esmp/infrastructure/health/HealthIndicatorIntegrationTest.java"
      to: "src/main/java/com/esmp/infrastructure/health/QdrantHealthIndicator.java"
      via: "Spring context loads health indicator bean"
      pattern: "health"
    - from: "src/test/java/com/esmp/infrastructure/health/HealthIndicatorIntegrationTest.java"
      to: "src/main/resources/application.yml"
      via: "Testcontainers override connection properties"
      pattern: "@ServiceConnection|@DynamicPropertySource"
    - from: "src/test/java/com/esmp/infrastructure/startup/DataStoreStartupValidatorTest.java"
      to: "src/main/java/com/esmp/infrastructure/startup/DataStoreStartupValidator.java"
      via: "Unit tests the fail-fast logic with mocked health indicators"
      pattern: "DataStoreStartupValidator"
---

<objective>
Create integration tests that prove all infrastructure components work together, then verify the full Docker Compose environment starts correctly.

Purpose: Validate that the skeleton from Plan 01 actually works end-to-end — health indicators report UP against real databases, virtual threads are enabled, Flyway migrations apply, Docker Compose brings up all services, and the fail-fast startup validator aborts when a store is unreachable.
Output: Passing integration tests + verified Docker environment.
</objective>

<execution_context>
@C:/Users/aziz.rehman/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aziz.rehman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-RESEARCH.md
@.planning/phases/01-infrastructure/01-VALIDATION.md
@.planning/phases/01-infrastructure/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that tests must validate -->

From src/main/java/com/esmp/infrastructure/health/QdrantHealthIndicator.java:
```java
@Component
public class QdrantHealthIndicator implements HealthIndicator {
    public Health health() { ... }
}
```

From src/main/java/com/esmp/infrastructure/config/QdrantConfig.java:
```java
@Configuration
public class QdrantConfig {
    @Bean
    public QdrantClient qdrantClient(...) { ... }
}
```

From src/main/java/com/esmp/infrastructure/startup/DataStoreStartupValidator.java:
```java
@Component
public class DataStoreStartupValidator implements ApplicationRunner {
    public DataStoreStartupValidator(List<HealthIndicator> healthIndicators) { ... }
    public void run(ApplicationArguments args) { ... } // throws IllegalStateException if any DOWN
}
```

From src/main/resources/application.yml:
```yaml
# Key properties tests need to override:
spring.neo4j.uri: bolt://localhost:7687
spring.datasource.url: jdbc:mysql://localhost:3306/esmp
qdrant.host: localhost
qdrant.port: 6333
management.endpoints.web.exposure.include: health, info, metrics, prometheus
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integration tests for health indicators, virtual threads, and fail-fast startup</name>
  <files>
    src/test/java/com/esmp/infrastructure/health/HealthIndicatorIntegrationTest.java,
    src/test/java/com/esmp/infrastructure/health/VirtualThreadsTest.java,
    src/test/java/com/esmp/infrastructure/startup/DataStoreStartupValidatorTest.java
  </files>
  <behavior>
    HealthIndicatorIntegrationTest:
    - Test 1: GET /actuator/health returns 200 with status UP
    - Test 2: Health response includes "neo4j" component with status UP
    - Test 3: Health response includes "db" (MySQL/datasource) component with status UP
    - Test 4: Health response includes "qdrant" component with status UP
    - Test 5: Flyway migration applied (migration_job table exists — query it via JdbcTemplate)

    VirtualThreadsTest:
    - Test 1: Application context loads successfully
    - Test 2: Current thread in a request handler is a virtual thread (Thread.currentThread().isVirtual())
    - Test 3: GET /actuator/health returns 200 (smoke test the endpoint is accessible)

    DataStoreStartupValidatorTest (unit test — no Testcontainers needed):
    - Test 1: When all health indicators return UP, run() completes without exception
    - Test 2: When one health indicator returns DOWN, run() throws IllegalStateException
    - Test 3: Exception message includes the name of the failing component
  </behavior>
  <action>
    Create `HealthIndicatorIntegrationTest.java`:
    - `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
    - `@Testcontainers` annotation
    - `@Container @ServiceConnection` for `Neo4jContainer<>("neo4j:2026.01.4")` — static field
    - `@Container @ServiceConnection` for `MySQLContainer<>("mysql:8.4")` — static field
    - `@Container` for `GenericContainer<>("qdrant/qdrant:latest")` with exposed ports 6333, 6334 and `Wait.forHttp("/healthz").forPort(6333)` — static field
    - `@DynamicPropertySource` for Qdrant properties (qdrant.host, qdrant.port) since Qdrant has no `@ServiceConnection` support
    - Inject `TestRestTemplate` (auto-configured with random port)
    - Test methods call GET `/actuator/health` and parse JSON response to assert component statuses
    - For Flyway test: inject `JdbcTemplate` and query `SELECT COUNT(*) FROM information_schema.tables WHERE table_name = 'migration_job'`

    Create `VirtualThreadsTest.java`:
    - `@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)`
    - `@Testcontainers` with same containers as above (or extract to a shared base class / use `@Import`)
    - Test that application context loads (simple `@Autowired ApplicationContext` not null assertion)
    - Test virtual threads: create a simple `@RestController` test endpoint or use `@Async` invocation and verify `Thread.currentThread().isVirtual()` — alternatively, check the actuator health endpoint responds (proves the app started with virtual threads since they're configured)
    - Simpler approach: verify the property is active by injecting `@Value("${spring.threads.virtual.enabled}") boolean virtualThreadsEnabled` and asserting true

    Create `DataStoreStartupValidatorTest.java`:
    - Plain JUnit 5 unit test (no Spring context, no Testcontainers)
    - Create mock `HealthIndicator` instances using Mockito or simple anonymous implementations
    - Test happy path: create `DataStoreStartupValidator` with a list of indicators that all return `Health.up()`, call `run(mock(ApplicationArguments.class))`, assert no exception thrown
    - Test failure path: create a list where one indicator returns `Health.down().withDetail("error", "connection refused").build()`, call `run(...)`, assert `IllegalStateException` is thrown
    - Test exception message: verify the thrown exception message contains a meaningful identifier (indicator class name or component name)
    - This is a UNIT test because the fail-fast logic is pure Java — no Spring context needed. The integration tests in HealthIndicatorIntegrationTest already prove the indicators work with real containers.

    NOTE: Both integration test classes will share the same Testcontainers setup. To avoid duplication, consider creating a shared abstract base class `AbstractIntegrationTest` with the container definitions, OR use a `@TestConfiguration` class. The executor should use the simplest approach that works — duplicate containers in both classes is acceptable for 2 test classes.

    IMPORTANT: Testcontainers requires Docker to be running. The tests will fail if Docker is not available. This is expected and documented.
  </action>
  <verify>
    <automated>cd C:/frontoffice/esmp && ./gradlew test --no-daemon 2>&1 | tail -20</automated>
  </verify>
  <done>
    All integration tests pass: health indicators for Neo4j, MySQL, and Qdrant all report UP. Virtual threads property is active. Flyway migration applied to MySQL. DataStoreStartupValidator unit tests confirm fail-fast behavior (throws on DOWN, passes on UP). `./gradlew test` exits with BUILD SUCCESSFUL.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Docker Compose environment and Spring Boot startup</name>
  <action>
    Human verifies that the complete ESMP infrastructure works end-to-end:
    - Complete ESMP infrastructure: Gradle project skeleton with Spring Boot 3.5.11 on Java 21, Docker Compose with Neo4j/Qdrant/MySQL/Prometheus/Grafana, custom health indicators, fail-fast startup validation, Flyway migration, structured logging profiles, and passing integration tests.

    Verification steps:
    1. Start Docker services: `docker compose up -d` from the project root
    2. Wait for all containers to be healthy: `docker compose ps` — all should show "healthy"
    3. Start Spring Boot: `./gradlew bootRun --args='--spring.profiles.active=dev'`
    4. Verify health endpoint: open http://localhost:8080/actuator/health in browser
       - Expected: JSON with `status: "UP"` and components for neo4j, db, qdrant all UP
    5. Verify Prometheus endpoint: open http://localhost:8080/actuator/prometheus
       - Expected: Prometheus metrics text output with JVM and Tomcat metrics
    6. Verify Grafana: open http://localhost:3000 (login: admin / admin)
       - Expected: Grafana UI loads (no dashboards yet — that is expected)
    7. Verify fail-fast: stop MySQL (`docker compose stop mysql`), then try `./gradlew bootRun` again
       - Expected: Application fails to start with error about data store being unreachable
    8. Restore: `docker compose start mysql` and then `docker compose down` to clean up
  </action>
  <verify>Human confirms all services start healthy, endpoints respond correctly, and fail-fast works when a store is stopped</verify>
  <done>User types "approved" confirming Docker Compose environment, Spring Boot, and fail-fast startup validation work end-to-end, or describes issues to fix.</done>
</task>

</tasks>

<verification>
1. `./gradlew test` passes all integration tests (health indicators + virtual threads + fail-fast unit test)
2. `./gradlew build` succeeds (compile + test + Spotless check)
3. `docker compose up -d` starts all 5 services to healthy state
4. `./gradlew bootRun` connects to all stores and reports healthy at /actuator/health
5. /actuator/prometheus returns Prometheus-formatted metrics
6. Stopping a data store and restarting the app causes startup failure (fail-fast validation)
</verification>

<success_criteria>
- `./gradlew test` BUILD SUCCESSFUL with all tests passing
- Integration tests prove Neo4j, MySQL, and Qdrant health indicators return UP against real containers
- DataStoreStartupValidator unit test proves fail-fast behavior (throws IllegalStateException on DOWN)
- Human verifies docker compose up + bootRun produces healthy environment
- Human verifies fail-fast by stopping MySQL and observing startup failure
- Actuator endpoints (health, info, metrics, prometheus) all accessible
- Phase 1 success criteria met: developer can run full environment locally
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-02-SUMMARY.md`
</output>
