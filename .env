# Local development environment variables
# These are safe defaults for local development only.
# If you add real secrets, add .env to .gitignore and create a .env.example template.
MYSQL_ROOT_PASSWORD=esmp-local-root
MYSQL_PASSWORD=esmp-local-password
NEO4J_PASSWORD=esmp-local-password
GRAFANA_PASSWORD=admin
